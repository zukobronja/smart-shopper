# Container commands for environment variable management
# Note: Direct environment variable substitution doesn't work reliably in AWS EB
# URLs are manually synchronized in environment.config for now